server:
  port: 2020
    #servlet:
    #contextPath:
  #/eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:2020/eureka
    fetch-registry: false
    register-with-eureka: false

    #instance:
    #hostname: localhost